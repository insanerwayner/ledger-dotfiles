#!/bin/bash
forecast="
~ every Friday  Weekly Weight
    (Assets:Weight)     -$1 LB
"
echo "$forecast" | hledger -f - -f ~/ledger/weight.ledger print assets -V --forecast | ledger -f - reg -H --tail 26
